import { css } from '@emotion/react'

const styleCard = css`
  display: flex;
  justify-content: center;
  font-size: 40px;
`

const Card = () => {
  return <div css={styleCard}>Test</div>
}
export default Card
